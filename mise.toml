# Machine-setup config, applied by `mise bootstrap` (run from bootstrap.sh).
# Loaded because the `bootstrap` shell function cd's into this repo first, so it
# merges with the global ~/.config/mise/config.toml (tools/settings/env) and the
# per-OS package files mise.macos.toml / mise.linux.toml (via MISE_ENV).
# Must be trusted: `mise trust`.

[dotfiles]
# Sources are relative to this file (~/.dotfiles). Default mode is symlink.
"~/.claude/CLAUDE.md" = "agents/AGENTS.md"
"~/.config/opencode/AGENTS.md" = "agents/AGENTS.md"
"~/.codex/AGENTS.md" = "agents/AGENTS.md"
"~/.claude/settings.json" = "agents/claude.json"
"~/.config/opencode/opencode.json" = "agents/opencode.json"
"~/.codex/config.toml" = "agents/codex.toml"
"~/.config/opencode/skills" = "agents/skills"
"~/.codex/skills" = "agents/skills"
"~/.config/ghostty" = "ghostty"
"~/.gitattributes" = "git/gitattributes"
"~/.gitconfig" = "git/gitconfig"
"~/.gitignore" = "git/gitignore"
"~/.config/kitty" = "kitty"
"~/.pdbrc.py" = "python/pdbrc.py"
"~/.config/starship.toml" = "starship/starship.toml"
"~/.config/nvim" = "vim"
"~/.zsh_plugins.txt" = "zsh/zsh_plugins.txt"
"~/.zshrc" = "zsh/zshrc"

[tasks.bootstrap]
description = "Project-specific setup that doesn't fit the declarative sections"
run = "poetry config virtualenvs.in-project true"

[bootstrap.hooks.final]
run = """
claude update || true
rtk init -g --hook-only --auto-patch || true
rtk init -g --hook-only --auto-patch --opencode || true
zsh -i -c "antidote update -b"
"""
