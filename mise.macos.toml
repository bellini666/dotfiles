[bootstrap.packages]
"brew-cask:1password-cli" = "latest"
"brew-cask:font-codicon" = "latest"
"brew-cask:font-fira-code-nerd-font" = "latest"
"brew-cask:font-hack-nerd-font" = "latest"
"brew-cask:font-inconsolata-nerd-font" = "latest"
"brew-cask:font-jetbrains-mono-nerd-font" = "latest"
"brew-cask:font-symbols-only-nerd-font" = "latest"
"brew-cask:hiddenbar" = "latest"
"brew-cask:karabiner-elements" = "latest"
"brew-cask:meetingbar" = "latest"
"brew:antidote" = "latest"
"brew:ast-grep" = "latest"
"brew:bat" = "latest"
"brew:bottom" = "latest"
"brew:btop" = "latest"
"brew:cheat" = "latest"
"brew:cjson" = "latest"
"brew:cmake" = "latest"
"brew:coreutils" = "latest"
"brew:cosign" = "latest"
"brew:cpanminus" = "latest"
"brew:curl" = "latest"
"brew:docker" = "latest"
"brew:docker-buildx" = "latest"
"brew:docker-compose" = "latest"
"brew:docker-credential-helper" = "latest"
"brew:docker-credential-helper-ecr" = "latest"
"brew:duf" = "latest"
"brew:eza" = "latest"
"brew:fd" = "latest"
"brew:ffmpegthumbnailer" = "latest"
"brew:fzf" = "latest"
"brew:gdal" = "latest"
"brew:gdb" = "latest"
"brew:gh" = "latest"
"brew:ghostscript" = "latest"
"brew:git" = "latest"
"brew:gitleaks" = "latest"
"brew:glab" = "latest"
"brew:gnu-sed" = "latest"
"brew:go-task" = "latest"
"brew:googleworkspace-cli" = "latest"
"brew:gradle" = "latest"
"brew:gradle-completion" = "latest"
"brew:graphicsmagick" = "latest"
"brew:htop" = "latest"
"brew:httpie" = "latest"
"brew:imagemagick" = "latest"
"brew:inetutils" = "latest"
"brew:jj" = "latest"
"brew:jq" = "latest"
"brew:k9s" = "latest"
"brew:kotlin" = "latest"
"brew:kubectx" = "latest"
"brew:kubernetes-cli" = "latest"
"brew:lazygit" = "latest"
"brew:lftp" = "latest"
"brew:libfaketime" = "latest"
"brew:libffi" = "latest"
"brew:libmagic" = "latest"
"brew:libmicrohttpd" = "latest"
"brew:libtool" = "latest"
"brew:libwebsockets" = "latest"
"brew:luarocks" = "latest"
"brew:make" = "latest"
"brew:md5sha1sum" = "latest"
"brew:mole" = "latest"
"brew:nats-server" = "latest"
"brew:ninja" = "latest"
"brew:nmap" = "latest"
"brew:ollama" = "latest"
"brew:openjdk@17" = "latest"
"brew:openssl@3" = "latest"
"brew:pgcli" = "latest"
"brew:pipx" = "latest"
"brew:poppler" = "latest"
"brew:postgresql@17" = "latest"
"brew:pup" = "latest"
"brew:readline" = "latest"
"brew:redis" = "latest"
"brew:ripgrep" = "latest"
"brew:sd" = "latest"
"brew:shellcheck" = "latest"
"brew:shfmt" = "latest"
"brew:snitch" = "latest"
"brew:sox" = "latest"
"brew:sqlite" = "latest"
"brew:starship" = "latest"
"brew:stylua" = "latest"
"brew:taplo" = "latest"
"brew:tectonic" = "latest"
"brew:tilt" = "latest"
"brew:ttyd" = "latest"
"brew:unar" = "latest"
"brew:unbound" = "latest"
"brew:universal-ctags" = "latest"
"brew:vhs" = "latest"
"brew:watch" = "latest"
"brew:watchman" = "latest"
"brew:weasyprint" = "latest"
"brew:wget" = "latest"
"brew:xmlsectool" = "latest"
"brew:xz" = "latest"
"brew:yq" = "latest"
"brew:zlib" = "latest"
"brew:zsh" = "latest"

[bootstrap.hooks.post-packages]
run = '''
command -v brew >/dev/null 2>&1 || { echo "brew not found" >&2; exit 1; }

# mise owns which formulae are needed (its closure plus `packages prune`).
# Stop brew's autoremove — run by the `brew cleanup`/`brew uninstall` calls
# below — from deleting closure deps mise still wants, e.g. a mid-transition
# dep like openssl@4 that no installed formula records yet. Otherwise mise
# repours it every run and cleanup deletes it again, an endless loop.
export HOMEBREW_NO_AUTOREMOVE=1

# mise's brew backend only overwrites links it created, so any formula not
# installed by mise (a brew-installed dependency, or a half-linked leftover
# from an earlier aborted run) fails `cannot link ...: files not created by
# mise or brew` when mise upgrades it. Unlink everything this upgrade is
# about to pour so mise relinks each fresh. Unconditional on purpose: a
# record-less orphan conflicts just the same, and `brew unlink` clears it
# regardless. Safe because the same run repours each formula (keg is kept).
mise bootstrap packages upgrade --manager brew --dry-run --yes 2>/dev/null |
  sed -n 's#^pour \([^/]*\)/.*#\1#p' |
  while IFS= read -r formula; do
    brew unlink "${formula}" || true
  done

mise bootstrap packages upgrade --yes
mise bootstrap packages prune --manager brew --yes

# mise prune only handles formulae; casks still need manual pruning
# against the brew-cask entries declared above.
DECLARED_CASKS=$(mise bootstrap packages status --json |
  jq -r '.["brew-cask"].packages[].package')
# jq exits 0 on empty input, so an upstream failure would leave every
# installed cask looking undeclared and uninstall the lot. This file always
# declares some, so empty means something broke.
if [ -z "${DECLARED_CASKS}" ]; then
  echo "no brew-cask entries reported; refusing to prune casks" >&2
  exit 1
fi

UNDECLARED_CASKS=$(brew list --cask 2>/dev/null | grep -vxF "${DECLARED_CASKS}" || true)
if [ -n "${UNDECLARED_CASKS}" ]; then
  # --force also removes phantom Caskroom dirs (e.g. tmp leftovers from
  # a failed mise cask install), which plain uninstall errors on.
  brew uninstall --cask --force ${UNDECLARED_CASKS}
fi

# mise prune removes undeclared formulae but leaves old keg versions and
# the download cache behind (it pours new versions alongside old ones);
# brew cleanup reclaims those, keeping only the currently-linked keg.
brew cleanup
'''
